# Hosts

Each directory is one machine.

- `t14` is the current ThinkPad.
- `i5-5060` is a prepared migration template for an Intel i5 desktop with RTX 5060.

A host file should stay small: import hardware config, import profiles, set the hostname, and set `system.stateVersion`.
